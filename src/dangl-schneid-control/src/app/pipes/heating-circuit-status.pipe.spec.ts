import { HeatingCircuitStatusPipe } from './heating-circuit-status.pipe';

describe('HeatingCircuitStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new HeatingCircuitStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
