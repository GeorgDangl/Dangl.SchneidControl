import { TransferStationStatusPipe } from './transfer-station-status.pipe';

describe('TransferStationStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new TransferStationStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
